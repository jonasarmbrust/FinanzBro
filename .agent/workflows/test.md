---
description: Run all tests and check for issues
---

// turbo-all

1. Run the full test suite with verbose output:

```powershell
python -m pytest tests/ -v --tb=short
```

2. If any tests fail, investigate and fix the issue before proceeding.
